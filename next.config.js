/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['i.imgur.com', 'nomadux.com','el.commonsupport.com'],
   
  },
  reactStrictMode: true,

}

module.exports = nextConfig
